#! /usr/bin/env bash

# Cosmic Toaster Clipboard :tm:
# On a headless server without a 'real' clipboard, this temp file
# and simple bash function operate as a poor-mans clipboard. Can
# transfer textual information between processes by reading/writing
# from stdin/stdout
touch "${CT_CLIPBOARD}"
command cat $CT_CLIPBOARD
